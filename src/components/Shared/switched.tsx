import * as React from "react";
import BasicSwitches, { SwitchProps } from "@mui/material/Switch";

const Switch = (props: SwitchProps) => {
  return <BasicSwitches {...props} />;
};
export default Switch;
